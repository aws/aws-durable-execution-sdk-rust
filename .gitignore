# Build artifacts
/target

# Generated conformance handler builds (Lambda bootstrap binaries and SAM
# build metadata produced by conformance/build_examples.sh). Never commit
# these: the binaries are ~15 MB each and the metadata embeds absolute
# local paths.
/conformance/build/

# Cargo.lock is checked in for reproducible builds of the conformance
# binary crate. For library crates the convention is to gitignore it,
# but this workspace contains a binary-like member (conformance handlers)
# whose exact dependency resolution matters for conformance runs.
# Decision: CHECK IN Cargo.lock (do not ignore it).
# Ref: https://doc.rust-lang.org/cargo/faq.html#why-have-cargolock-in-version-control

# Editor/IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
tokio_join_test

# Agent scratch space. Kept on disk so a later step can read an earlier
# step's notes, never committed.
.agents/
.kiro/
