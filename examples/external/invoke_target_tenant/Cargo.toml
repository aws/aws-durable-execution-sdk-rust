[package]
name = "invoke_target_tenant"
version = "0.0.0"
edition = "2024"
rust-version = "1.94.1"
publish = false
description = "Example companion: tenant-isolated echo target for chained invoke"

[[bin]]
name = "invoke_target_tenant"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
aws-durable-execution-sdk-rust = { path = "../../.." }
lambda_runtime = "1"
serde_json = "1"
tokio = { version = "1", features = ["macros"] }
