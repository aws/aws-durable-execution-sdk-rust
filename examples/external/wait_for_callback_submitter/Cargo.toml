[package]
name = "wait_for_callback_submitter"
version = "0.0.0"
edition = "2024"
rust-version = "1.94.1"
publish = false
description = "Example: wait_for_callback with a submitter and retry"

[[bin]]
name = "wait_for_callback_submitter"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
aws-durable-execution-sdk = { path = "../../.." }
lambda_runtime = "1"
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
tracing = "0.1"
